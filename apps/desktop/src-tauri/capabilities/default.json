{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for bootstrap desktop app",
  "windows": ["main"],
  "permissions": ["core:default"]
}
